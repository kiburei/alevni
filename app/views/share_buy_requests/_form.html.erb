<%= form_for(@share_buy_request, html: { class: 'form-horizontal'}) do |f| %>
  <% if @share_buy_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@share_buy_request.errors.count, "error") %> prohibited this share_buy_request from being saved:</h2>

      <ul>
      <% @share_buy_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <div class="col-sm-10">
      <%= f.hidden_field :property_id, :value => params[:property_id] %>
  </div>
</div>

  <div class="field form-group">
    <div class="col-sm-10">
    <%= f.hidden_field :share_sell_request_id, :value => params[:share_sell_request_id] %>
  </div>
  </div>
  <div class="field form-group">
    <!-- <%= f.label :no_of_shares %>: -->
    <%= f.text_field :no_of_shares, :placeholder => "Shares to Buy" %>
  </div>
  <div class="actions form-group">
    <div class="col-sm-offset-2 col-sm-10">
    <%= f.submit %>
  </div>
  </div>
<% end %>
